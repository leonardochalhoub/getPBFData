// Optional helper file to keep Firebase credentials out of app.js\n//\n// 1) Create a Firebase project + Firestore\n// 2) Create a Web App in Firebase Console\n// 3) Paste the firebaseConfig object below\n//\n// This file is loaded by index.html before app.js and sets window.FIREBASE_CONFIG.\n//\n// IMPORTANT:\n// - These credentials are NOT secret (they are used in the browser).\n// - Security must be enforced via Firestore Rules.\n\nwindow.FIREBASE_CONFIG = {\n  apiKey: \"REPLACE_ME\",\n  authDomain: \"REPLACE_ME\",\n  projectId: \"REPLACE_ME\",\n};\n
