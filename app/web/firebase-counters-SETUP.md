# Firebase counters (GitHub Pages) — Setup automático (passo a passo)\n\nEste site roda em **GitHub Pages** (100% estático). Para ter contadores globais (visitas/downloads) a forma mais simples é usar **Firebase Firestore**.\n\nA parte “automática” possível aqui é:\n- deixar o site pronto para receber as credenciais do Firebase via **um único arquivo local** (`firebase-config.js`), sem precisar editar `app.js`;\n- documentar um setup **copiar/colar** no Firebase Console;\n- (opcional) gerar regras do Firestore prontas.\n\n> Observação: não dá para eu criar o projeto Firebase por você “de dentro do repositório” sem você executar `firebase` CLI e autenticar com sua conta Google. Por isso, a automação aqui é no código + instruções bem diretas.\n\n---\n\n## 1) Criar projeto no Firebase\n\n1. Acesse: https://console.firebase.google.com/\n2. **Add project**\n3. Nome sugerido: `getpbfdata-counters` (ou qualquer outro)\n4. Pode desativar Google Analytics para simplificar.\n\n---\n\n## 2) Criar Firestore\n\n1. No menu do projeto: **Build → Firestore Database**\n2. Clique **Create database**\n3. Modo: **Start in production mode**\n4. Localização: escolha a mais próxima (ex.: `southamerica-east1` se disponível, senão `us-east1`)\n\n---\n\n## 3) Criar um Web App no Firebase (para obter credenciais)\n\n1. No Overview do projeto, clique no ícone **</> Web**\n2. App nickname: `getPBFData-web`\n3. NÃO precisa marcar “Firebase Hosting”\n4. Ao final, copie o objeto `firebaseConfig`.\n\n---\n\n## 4) Criar documento inicial no Firestore\n\nCrie (ou deixe o código criar automaticamente) o documento:\n\n- Collection: `counters`\n- Document: `public`\n\nCampos:\n- `visits` (number) = `0`\n- `downloads` (number) = `0`\n\n> O código tenta criar/atualizar via `set(..., {merge:true})`, então mesmo se não existir ele tende a “nascer” quando o 1º usuário abrir o site.\n\n---\n\n## 5) Regras do Firestore (importante)\n\nVocê precisa permitir que qualquer visitante **leia** os contadores e **incremente** com transação.\n\nNo Firestore → **Rules**, cole algo assim:\n\n```js\nrules_version = '2';\nservice cloud.firestore {\n  match /databases/{database}/documents {\n    match /counters/{docId} {\n      // Permite ler sempre\n      allow read: if true;\n\n      // Permite criar/atualizar APENAS este doc \"public\" e apenas campos numéricos\n      allow create, update: if docId == 'public'\n        && request.resource.data.keys().hasOnly(['visits', 'downloads'])\n        && request.resource.data.visits is int\n        && request.resource.data.downloads is int;\n    }\n  }\n}\n```\n\nClique **Publish**.\n\n---\n\n## 6) Arquivo `firebase-config.js`\n\nCrie um arquivo novo no repo (mesma pasta do `index.html`):\n\n`app/web/firebase-config.js`\n\ncom o conteúdo (substitua pelos seus valores):\n\n```js\n// Gerado no Firebase Console → Project settings → Your apps → Web app\nwindow.FIREBASE_CONFIG = {\n  apiKey: \"...\",\n  authDomain: \"...\",\n  projectId: \"...\",\n};\n```\n\n---\n\n## 7) Deploy\n\nFaça commit + push para o GitHub, e o GitHub Pages vai servir a página já com os contadores.\n\n---\n\n## Checklist rápido\n\n- [ ] Firebase project criado\n- [ ] Firestore criado\n- [ ] Web App criado e `firebaseConfig` copiado\n- [ ] Rules publicadas\n- [ ] `app/web/firebase-config.js` criado e commitado\n- [ ] GitHub Pages atualizado\n\n---\n\n## Dica (segurança)\n\nAs credenciais do Firebase Web App **não são segredo** (vão para o browser). Segurança real vem das **Rules**.\n\n"
<task_progress>
- [x] Review current web page structure and where to display counters
- [x] Decide counter storage approach (client-only vs persisted backend) and implement
- [x] Implement visitor counter increment on page load
- [x] Implement download counter increment on download action(s)
- [x] Update UI/styles to show counters nicely
- [x] Validate locally (open page / run container if applicable)
</task_progress>
</write_to_file>
